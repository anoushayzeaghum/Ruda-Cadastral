/**
 * Flood layer placeholder for the Hydrology group.
 *
 * This is intentionally UI-only for now. No API endpoint, GeoJSON source,
 * Mapbox source/layer, or backend data is connected yet.
 */
export const FLOOD_LAYER = {
  key: "flood",
  label: "Flood",
  color: "#38bdf8",
  dataAvailable: false,
};

export default FLOOD_LAYER;
